import 'package:flutter/material.dart';

const pColor = Color(0xFF9C5F28); // สีสำหรับประกาศคำแถลง

const sColor = Color(0xFF9C5F28); // สีสำหรับการรับรอง

const tColor = Color(0xFF8D9D43); //สีสำหรับตรวจสอบ

const uColor = Color(0xFFC4D285); // สีสำหรับในการให้บริการ

const bgColor = Color(0xFFEAEED7); // สีสำหรับพื้นหลังหน้าจอหลัก

const gColor = Color(0xFFE9CBB1);

const boxSize = 20.0;
